# 🔧 Решение проблемы конфликта бота

## Проблема
Бот не отвечает на `/start` из-за конфликта: запущено несколько экземпляров бота с одним токеном.

## Решение

### Вариант 1: Остановить бота на сервере (рекомендуется для локального теста)

Подключитесь к серверу и остановите бота:

```bash
# Через SSH
ssh root@109.69.16.218

# На сервере
cd ~/telegram-bot
pkill -f "python.*bot.py"
rm -f bot.lock bot.pid
```

Затем запустите бота локально:

```bash
./start_bot.sh
```

### Вариант 2: Использовать только локальный бот

Если хотите тестировать только локально, остановите бота на сервере и запустите локально.

### Вариант 3: Использовать webhook вместо polling

Если нужно, чтобы бот работал и на сервере, и локально, используйте webhook для сервера.

## Проверка

После остановки бота на сервере, запустите локально:

```bash
./start_bot.sh
```

Проверьте логи:

```bash
tail -f bot.log
```

Должно быть:
```
🚀 Бот запущен...
📢 Канал: @test_videcode
✅ Ожидаю обновления...
```

Теперь бот должен отвечать на `/start` в Telegram!
