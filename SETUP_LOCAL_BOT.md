# 🤖 Настройка локального бота для теста

## Проблема
Текущий токен бота (`7976749549:AAGrbBfOv563pTfWSDVvundxhE4ApX05-7s`) уже используется на удаленном сервере, поэтому возникает конфликт.

## Решение: Создать нового бота для локального теста

### Шаг 1: Создайте нового бота через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `SpaceX Exchange Local Test`)
4. Введите username бота (например: `spacex_local_test_bot`)
5. Скопируйте токен, который даст BotFather

### Шаг 2: Обновите .env файл

Откройте файл `.env` и замените токен:

```bash
BOT_TOKEN=новый_токен_от_BotFather
CHANNEL_ID=@test_videcode
MINI_APP_URL=http://localhost:8000
```

### Шаг 3: Запустите бота локально

```bash
./start_bot.sh
```

Готово! Теперь у вас будет отдельный бот для локального теста, который не конфликтует с серверным.
