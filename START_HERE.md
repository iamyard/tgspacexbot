# 🚀 Быстрый старт - ВСЁ ГОТОВО!

## ✅ Что уже сделано:

1. ✅ Установлены все зависимости (FastAPI, uvicorn, aiogram и др.)
2. ✅ Создано виртуальное окружение
3. ✅ Установлен ngrok
4. ✅ Создана структура Mini App
5. ✅ Обновлен бот для работы с Mini App
6. ✅ Создан файл .env (нужно заполнить)

## 📝 ШАГ 1: Заполните .env файл

Откройте файл `.env` и заполните:

```bash
BOT_TOKEN=ваш_токен_бота_от_BotFather
CHANNEL_ID=@ваш_канал или -1001234567890
MINI_APP_URL=http://localhost:8000  # Будет обновлен автоматически
```

## 🚀 ШАГ 2: Запустите всё одной командой

```bash
./run_all.sh
```

Этот скрипт автоматически:
- Проверит настройки
- Запустит ngrok туннель
- Обновит MINI_APP_URL в .env
- Запустит веб-сервер Mini App

## 📱 ШАГ 3: Откройте бота в Telegram

1. Найдите вашего бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку **"🎨 Редактор (Mini App)"**
4. Готово! 🎉

## 🔧 Альтернативный запуск (по отдельности)

### Терминал 1: ngrok
```bash
ngrok http 8000
```
Скопируйте HTTPS URL и добавьте в .env как MINI_APP_URL

### Терминал 2: Веб-сервер
```bash
source venv/bin/activate
python3 -m uvicorn webapp.main:app --host 0.0.0.0 --port 8000 --reload
```

### Терминал 3: Telegram бот (опционально)
```bash
source venv/bin/activate
python3 bot.py
```

## 📋 Полезные команды

- `./setup_miniapp.sh` - Интерактивная настройка
- `./run_all.sh` - Запуск всего автоматически
- `./start_miniapp_local.sh` - Только веб-сервер

## 🐛 Решение проблем

**Mini App не открывается:**
- Убедитесь, что MINI_APP_URL начинается с `https://`
- Проверьте, что ngrok запущен: `pgrep -f ngrok`

**Ошибка "BOT_TOKEN не найден":**
- Заполните BOT_TOKEN в файле .env

**Веб-сервер не запускается:**
- Убедитесь, что порт 8000 свободен: `lsof -i :8000`

## 📚 Документация

- `README_MINIAPP.md` - Подробная документация
- `QUICKSTART_MINIAPP.md` - Быстрый старт

---

**Готово к использованию!** 🎉
