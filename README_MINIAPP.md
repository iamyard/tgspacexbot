# Telegram Mini App - Инструкция по запуску

## 🎯 Что это?

Mini App - это веб-интерфейс внутри Telegram, который позволяет удобно редактировать курсы валют и создавать посты/сторис без пошагового ввода через бота.

## 📋 Требования

1. **Python 3.8+**
2. **ngrok** (для локального теста с HTTPS)
3. **Telegram бот** с токеном

## 🚀 Быстрый старт (локальный тест)

### Шаг 1: Установите ngrok

**macOS:**
```bash
brew install ngrok/ngrok/ngrok
```

**Или скачайте с:** https://ngrok.com/download

### Шаг 2: Запустите ngrok в отдельном терминале

```bash
ngrok http 8000
```

Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

### Шаг 3: Настройте .env файл

Добавьте в `.env`:
```
MINI_APP_URL=https://ваш-ngrok-url.ngrok.io
```

### Шаг 4: Запустите Mini App

```bash
chmod +x start_miniapp_local.sh
./start_miniapp_local.sh
```

### Шаг 5: Откройте бота в Telegram

1. Найдите вашего бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку **"🎨 Редактор (Mini App)"**
4. Откроется веб-интерфейс!

## 📁 Структура проекта

```
tgspacexbot/
├── webapp/
│   ├── main.py              # FastAPI сервер
│   └── static/
│       ├── index.html       # HTML интерфейс
│       ├── style.css        # Стили
│       └── app.js           # JavaScript логика
├── bot.py                   # Telegram бот (обновлен)
├── config.py                # Конфигурация (добавлен MINI_APP_URL)
├── start_miniapp_local.sh   # Скрипт запуска
└── requirements.txt         # Зависимости (обновлен)
```

## 🔧 Ручной запуск

Если хотите запустить вручную:

### Терминал 1: ngrok
```bash
ngrok http 8000
```

### Терминал 2: Веб-сервер
```bash
source venv/bin/activate
python3 -m uvicorn webapp.main:app --host 0.0.0.0 --port 8000 --reload
```

### Терминал 3: Telegram бот
```bash
source venv/bin/activate
python3 bot.py
```

## 🌐 Деплой на продакшн

Для продакшна нужно:

1. **Настроить HTTPS** на вашем домене (Let's Encrypt)
2. **Обновить MINI_APP_URL** в `.env` на ваш домен
3. **Запустить веб-сервер** как системный сервис (systemd)
4. **Настроить Nginx** как reverse proxy (опционально)

### Пример systemd сервиса для веб-сервера:

```ini
[Unit]
Description=Telegram Mini App Web Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/telegram-bot
Environment="PATH=/root/telegram-bot/venv/bin"
ExecStart=/root/telegram-bot/venv/bin/uvicorn webapp.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

## 🐛 Решение проблем

### Mini App не открывается
- Проверьте, что URL начинается с `https://`
- Убедитесь, что ngrok запущен и работает
- Проверьте логи веб-сервера

### Ошибка "Connection refused"
- Убедитесь, что веб-сервер запущен на порту 8000
- Проверьте, что ngrok указывает на правильный порт

### Бот не отправляет в канал
- Проверьте, что бот добавлен как администратор канала
- Убедитесь, что CHANNEL_ID правильный в .env

## 📝 API Endpoints

- `GET /` - Главная страница Mini App
- `POST /api/generate-preview` - Генерация превью изображения
- `POST /api/send-to-channel` - Отправка в канал
- `GET /api/health` - Проверка здоровья сервера

## 🎨 Функционал Mini App

- ✅ Ввод всех курсов валют на одной странице
- ✅ Превью поста перед отправкой
- ✅ Превью сторис
- ✅ Отправка поста в канал одним кликом
- ✅ Скачивание сторис для Instagram
- ✅ Сохранение последних значений в localStorage
