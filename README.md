# Telegram Image Bot

Бот для генерации изображений с обновленными данными и постинга в телеграм канал с инлайн кнопками.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd telegram-image-bot
pip install -r requirements.txt
```

### 2. Настройка

1. **Убедитесь, что бот добавлен как администратор** в канал `@test_vibecode`:
   - Откройте настройки канала (Settings)
   - Перейдите в Administrators
   - Нажмите Add Administrator
   - Найдите вашего бота и добавьте его
   - Дайте боту права на отправку сообщений

2. **Файл `.env` уже настроен** с вашим токеном и каналом

### 3. Запуск

```bash
python bot.py
```

## 📋 Команды бота

- `/start` - Начать работу с ботом
- `/generate` - Сгенерировать и отправить изображение в канал
- `/update` - Обновить данные (пример)
- `/help` - Показать справку

## 🎨 Функционал

- ✅ Генерация красивых изображений с данными
- ✅ Отправка в телеграм канал `@test_vibecode`
- ✅ Инлайн кнопки для взаимодействия:
  - 🔄 Обновить данные
  - 📊 Подробнее
  - ❌ Удалить пост

## 🔧 Настройка данных

Данные для отображения находятся в файле `bot.py` в переменной `current_data`. 
Вы можете изменить их или подключить к базе данных/API.

## 📝 Структура проекта

```
telegram-image-bot/
├── bot.py              # Основной файл бота
├── image_generator.py  # Генерация изображений
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости
├── .env               # Переменные окружения
└── README.md          # Документация
```

## ⚠️ Важно

- Не коммитьте файл `.env` в репозиторий (он уже в `.gitignore`)
- Бот должен быть администратором канала для отправки сообщений
- Канал настроен: `@test_vibecode`
